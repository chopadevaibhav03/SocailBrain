package com.social.repo;

import org.springframework.data.jpa.repository.JpaRepository;

import com.social.model.Tag;

public interface TagRepository extends JpaRepository<Tag, Integer>{

    Tag saveTag(Tag tag);

}
